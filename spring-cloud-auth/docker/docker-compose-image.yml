version: '3'
services:
  auth:
    image: ${IMAGE_NAME}
    container_name: ${IMAGE_NAME}
    networks:
      - net
    ports:
      - 7001:7001
    environment:
      - JAVA_OPTS=-Xmx100M -Duser.timezone=GMT+08
      - NACOS_PUBLIC_IP=${NACOS_PUBLIC_IP}

networks:
  net:
    driver: bridge