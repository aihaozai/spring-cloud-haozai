<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.spring.cloud.auth.user.mapper.UserMapper">

    <!-- 通用查询结果列-->
    <sql id="Base_Column_List">
        id, username, password
    </sql>

    <select id="selectUserPage" resultType="com.spring.cloud.auth.user.entity.User">
        SELECT * FROM user
    </select>
</mapper>