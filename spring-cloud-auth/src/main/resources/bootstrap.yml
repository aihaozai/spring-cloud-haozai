server:
  port: 7001
  servlet:
    context-path: /auth

spring:
  profiles:
    active: dev,datasource
  application:
    name: cloud-oauth
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name:  com.p6spy.engine.spy.P6SpyDriver
    url: jdbc:p6spy:mysql://localhost:3306/cloud?useUnicode=true&characterEncoding=utf8&characterSetResults=utf8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: haozai

#swagger扫描配置
swagger:
  doc:
    enable: true
    basePackage: com.spring.cloud.auth
    title: 授权服务
    description:
    termsOfServiceUrl:
    version: 1.0

logging:
  level:
    org.springframework.security: DEBUG